server:
  port: 8890

spring:
  application:
    name: profile
  datasource:
    url: jdbc:postgresql://localhost:5433/assistant_db
    username: postgres
    password: postgres
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate.hibernate.format_sql: true
  #Туннелирование запроса DELETE через POST
  mvc:
    hiddenmethod:
      filter:
        enabled: true

management:
  endpoints:
    web:
      exposure:
        include: "metrics, health"
  endpoint:
    health:
      show-details: always

app:
  jwt:
    secret: "otusotusotusotusotusotusotusotus"
    expiration-times: 30m
    cookie-name: PROFILE_JWT_TOKEN
    secure-cookie: true
  default-password: "1111"
